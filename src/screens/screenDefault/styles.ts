import styled from 'styled-components/native';

const Container = styled.View`
  background-color: #fff;
`;
const WrapperHeader = styled.View`
  margin-top: 0px;
`;
const Text = styled.Text`
  font-size: 12px;
  color: red;
`;

/**
 * EXPORT
 */
export {Container, WrapperHeader, Text};
